const $=selector=>document.querySelector(selector);const cm=()=>['cloud-preview','cloud'].includes(globalThis.FlowboardApp?.getMode?.()?.kind);const mn=m=>m.displayName||m.emailLower||'Workspace member';const dt=value=>{const date=value?.toDate?.()||value;const result=date instanceof Date?date:new Date(date);return Number.isNaN(result.valueOf())?null:result;};const ist=i=>i.acceptedAt?'accepted':i.revokedAt?'revoked':dt(i.expiresAt)?.getTime()<=Date.now()?'expired':'pending';const sl=state=>state[0].toUpperCase()+state.slice(1);export function initializeMembersUI(adapter){const open=$('#open-workspace-members'),d=$('#workspace-members-dialog'),close=$('#close-workspace-members');const st=$('#workspace-members-status'),ml=$('#workspace-members-list');const form=$('#create-invite-form'),email=$('#invite-email'),role=$('#invite-role');const isec=$('#workspace-invites-section'),il=$('#workspace-invites-list'),ls=$('#invite-link-status');const tf=$('#transfer-ownership-form'),succ=$('#ownership-successor'),fr=$('#former-owner-role');let s=null,id=null,o=false;const msg=text=>{st.textContent=text;};const active=()=>cm()?globalThis.FlowboardApp.getMode():null;const confirm=({title,detail,label})=>new Promise(resolve=>{const prompt=document.createElement('dialog'),card=document.createElement('div'),heading=document.createElement('h2'),text=document.createElement('p'),actions=document.createElement('div'),cancel=document.createElement('button'),confirm=document.createElement('button');prompt.className='dialog';prompt.setAttribute('aria-labelledby','member-action-heading');card.className='archive-dialog-card';heading.id='member-action-heading';heading.textContent=title;text.textContent=detail;actions.className='dialog-actions';cancel.type='button';cancel.className='button button-quiet';cancel.textContent='Cancel';confirm.type='button';confirm.className='button button-primary';confirm.textContent=label;actions.append(cancel,confirm);card.append(heading,text,actions);prompt.append(card);document.body.append(prompt);const finish=result=>{prompt.close();prompt.remove();resolve(result);};cancel.onclick=()=>finish(false);confirm.onclick=()=>finish(true);prompt.oncancel=event=>{event.preventDefault();finish(false);};prompt.showModal();cancel.focus();});const copy=async text=>{try{if(!navigator.clipboard)throw Error('clipboard unavailable');await navigator.clipboard.writeText(text);ls.textContent='Invitation link copied.';}catch{ls.textContent=`Copy this invitation link: ${text}`;}};const inviteRow=i=>{const state=ist(i),row=document.createElement('div'),title=document.createElement('strong'),detail=document.createElement('span');row.className='workspace-board';row.dataset.inviteStatus=state;title.textContent=`${i.role || 'member'} invitation`;const expiry=dt(i.expiresAt);detail.textContent=`${i.emailLower || 'Recipient'} · ${sl(state)}${state === 'pending' && expiry ? `·expires ${expiry.toLocaleDateString()}` : ''}`;row.append(title,detail);if(state==='pending'){const copyButton=document.createElement('button'),revoke=document.createElement('button');copyButton.type='button';copyButton.className='button button-quiet';copyButton.textContent='Copy link';copyButton.onclick=()=>{const url=new URL(window.location.href);url.searchParams.set('workspace',id);url.searchParams.set('invite',i.id);copy(url.toString());};revoke.type='button';revoke.className='button button-quiet';revoke.textContent='Revoke';revoke.onclick=async()=>{if(!await confirm({title:'Revoke invitation?',detail:'This invitation link will stop working immediately.',label:'Revoke invitation'}))return;revoke.disabled=true;try{await adapter.revokeInvite(id,i.id);await r();msg('Invitation revoked.');}catch(e){console.error('Invite revoke failed.',e);msg('Invitation revocation failed. Local data unchanged.');}finally{revoke.disabled=false;}};row.append(copyButton,revoke);}return row;};const r=async()=>{const mode=active();id=mode?.id||null;if(!s||!id){open.hidden=true;if(d.open)d.close();return;}open.hidden=false;msg('Loading workspace access…');ml.replaceChildren();il.replaceChildren();try{const members=await adapter.listMembers(id),self=members.find(item=>item.uid===s.uid);if(!self)throw Error('not-member');o=self.role==='owner';members.forEach(m=>{const row=document.createElement('div'),title=document.createElement('strong'),detail=document.createElement('span');row.className='workspace-board';title.textContent=`${mn(m)}${m.uid === s.uid ? ' (you)' : ''}`;detail.textContent=m.role;row.append(title,detail);if(o&&m.role!=='owner'){const select=document.createElement('select');select.setAttribute('aria-label',`Change role for ${mn(m)}`);['editor','viewer'].forEach(value=>select.add(new Option(value,value,false,value===m.role)));select.onchange=async()=>{const next=select.value;if(!await confirm({title:'Change member role?',detail:`Change this member from ${m.role} to ${next}.`,label:'Change role'})){select.value=m.role;return;}select.disabled=true;try{await adapter.changeMemberRole(id,m.uid,next);await r();msg('Member role updated.');}catch(e){console.error('Role change failed.',e);select.value=m.role;msg('Role change failed. Local data unchanged.');}finally{select.disabled=false;}};const remove=document.createElement('button');remove.type='button';remove.className='button button-quiet';remove.textContent='Remove';remove.setAttribute('aria-label',`Remove ${mn(m)}`);remove.onclick=async()=>{if(!await confirm({title:'Remove member?',detail:'They will immediately lose this cloud workspace access. Local browser data is unaffected.',label:'Remove member'}))return;remove.disabled=true;try{await adapter.removeMember(id,m.uid);await r();msg('Member removed.');}catch(e){console.error('Member removal failed.',e);msg('Member removal failed. Local data unchanged.');}finally{remove.disabled=false;}};row.append(select,remove);}else if(!o&&m.uid===s.uid){const leave=document.createElement('button');leave.type='button';leave.className='button button-quiet';leave.textContent='Leave workspace';leave.onclick=async()=>{if(!await confirm({title:'Leave workspace?',detail:'You will lose cloud workspace access. This browser’s local workspace is unaffected.',label:'Leave workspace'}))return;leave.disabled=true;try{await adapter.leaveWorkspace(id);globalThis.FlowboardApp.returnToLocal();d.close();open.hidden=true;}catch(e){console.error('Workspace leave failed.',e);msg('You could not leave this workspace. No local data changed.');leave.disabled=false;}};row.append(leave);}ml.append(row);});form.hidden=!o;isec.hidden=!o;tf.hidden=!o;succ.replaceChildren();members.filter(m=>m.uid!==s.uid&&['editor','viewer'].includes(m.role)).forEach(m=>succ.add(new Option(mn(m),m.uid)));tf.querySelector('button[type="submit"]').disabled=!succ.options.length;if(o){const invites=await adapter.listInvites(id);invites.forEach(i=>il.append(inviteRow(i)));if(!invites.length)il.innerHTML='<p class="empty">No invitations yet.</p>';}msg(o?'Owner access: create invitations and manage editor/viewer access.':`You have ${self.role} access.`);}catch(e){console.error('Flowboard member administration failed.',e);msg('Workspace access could not be loaded. Local data unchanged.');open.hidden=true;}};window.addEventListener('flowboard:cloud-preview-change',r);open.addEventListener('click',async()=>{await r();if(!d.open&&!open.hidden)d.showModal();});close.addEventListener('click',()=>d.close());d.addEventListener('cancel',event=>{event.preventDefault();d.close();});d.addEventListener('close',()=>open.focus());form.addEventListener('submit',async event=>{event.preventDefault();if(!o||!id)return;const submit=form.querySelector('button[type="submit"]');submit.disabled=true;ls.textContent='Creating invitation…';try{const result=await adapter.createInvite({workspaceId:id,email:email.value,role:role.value,baseUrl:window.location.href});email.value='';await copy(result.url);await r();}catch(e){console.error('Invite create failed.',e);ls.textContent='Invitation creation failed. Local data unchanged.';}finally{submit.disabled=false;}});tf.addEventListener('submit',async event=>{event.preventDefault();if(!o||!id||!succ.value)return;if(!await confirm({title:'Transfer ownership?',detail:'The selected member becomes owner and your role will change. This cannot be undone without the new owner.',label:'Transfer ownership'}))return;const submit=tf.querySelector('button[type="submit"]');submit.disabled=true;msg('Transferring ownership…');try{await adapter.transferOwnership({workspaceId:id,successorUid:succ.value,formerOwnerRole:fr.value});msg('Ownership transferred. Your local workspace remains unchanged.');await r();}catch(e){console.error('Ownership transfer failed.',e);msg('Ownership transfer failed. Local data unchanged.');}finally{submit.disabled=false;}});return{setSession(next){s=next;if(!s&&d.open)d.close();r();}};}